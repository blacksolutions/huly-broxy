[defaults]
inventory = inventory.yml
host_key_checking = True
retry_files_enabled = False
# Set vault password via env: export ANSIBLE_VAULT_PASSWORD_FILE=ansible/.vault-pass
# or pass --vault-password-file on the CLI. See ansible/README.md.

[ssh_connection]
pipelining = True
